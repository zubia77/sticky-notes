{
  "name": "sticky-notes",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "api": "json-server --watch ./src/data/notes.json --port 5000",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "run-p api dev",
    "deploy": "git pull && npm i && npm run build && npm start"
  },
  "dependencies": {
    "api": "^4.5.1",
    "axios": "^0.27.2",
    "json-server": "^0.17.3",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "sass": "^1.52.1"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^1.3.0",
    "npm-run-all": "^4.1.5",
    "vite": "^2.9.9"
  }
}
